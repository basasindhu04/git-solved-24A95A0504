app:
  name: "DevOps Simulator"
  version: "2.0"

environment:
  default: "production"

profiles:
  production:
    logLevel: "info"
    enableMonitoring: true

  development:
    logLevel: "debug"
    enableMonitoring: true
    hotReload: true
